import requests
import time
import json
import Queue
import utils

# s = utils.Sql()
# query = "REPLACE INTO board (bid, btitle, bname, total) VALUES (%s, %s, %s, %s)"
# param = (23, "fsdaf", "asdfdf", 342)
# s.execute(query, param)
#
# query = "SELECT * FROM board"
# s = utils.Sql()
# r = s.searchall(query)
# print r, "\n\n\n"
# for i in r:
#     print i


a = True
print not a